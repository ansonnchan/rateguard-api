services:
  api:
    # FastAPI service.
    build: .
    container_name: rateguard-api
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  postgres:
    # Stores request log history.
    image: postgres:16-alpine
    container_name: rateguard-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: rateguard
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./sql/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d rateguard"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    # Sliding-window state store.
    image: redis:7-alpine
    container_name: rateguard-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  # Persistent Postgres data for local development.
  postgres_data:
